// Import the SmartSchedulerAgent from the Agents folder
import SmartSchedulerAgent from './Agents/SmartSchedulerAgent.js';

// Initialize the Smart Scheduler Agent
window.smartScheduler = new SmartSchedulerAgent(window.aiPlanner); 